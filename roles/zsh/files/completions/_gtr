#compdef gtr
local git_root=$(git rev-parse --show-toplevel)
if [[ -n "$git_root" ]]; then
  local subcmds=( 
    "web" 
    "landingpage" 
    $(find "$git_root/services" -type d -maxdepth 1 -mindepth 1 | sed "s|$git_root/||") 
  )
  _describe 'command' subcmds
fi
