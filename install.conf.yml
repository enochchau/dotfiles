- clean:
    ~/:
    ~/.config:
      recursive: true
       
- link:
    ~/.config/alacritty: 
        create: true
        path: home/config/alacritty
    ~/Library/Application Support/Code/User/settings.json:
        if: '[ `uname` = Darwin ]'
        create: true
        path: home/vscode-settings.json
    ~/.config/Code/User/settings.json:
        if: '[ `uname` = Linux ]'
        create: true
        path: home/vscode-settings.json
    ~/.config/nixpkgs:
        create: true
        path: home/config/nixpkgs
